<resources>
    <style name="AuthButtonStyle">
        <item name="android:background">@drawable/button_background</item> <!-- Цвет фона -->
        <item name="android:textColor">@color/white</item>
        <item name="android:layout_width">wrap_content</item> <!-- Ширина -->
        <item name="android:layout_height">wrap_content</item> <!-- Высота -->
        <item name="android:width">240dp</item> <!-- Ширина -->
        <item name="android:height">40dp</item>
        <item name="android:textSize">15sp</item>
    </style>
</resources>